# Build artifacts
build/
dist/
*.spec

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Compiled Go binaries (rebuild with: cd lib && make)
lib/encoder
lib/decoder

# Compiled binaries
*.so
*.o
*.a

# Vendored dependencies (install via pip/go mod instead)
cryptography/

# Test artifacts
test.mk4
tests/fixtures/*.mp4
test-results/
tests/e2e/report/
.testvenv/

# Node
node_modules/
ui/node_modules/
ui/dist/

# OS
.DS_Store
Thumbs.db

# Local secrets for docker compose (provider API keys)
.env
.env.local

# Playwright scratch node storage
.pw-collective/
test-results/

# Runtime data & logs (local node + fuse mount; never commit)
.cfs/
.collective/
.git-remotes/
*.log
